class AppConstants {
  static const String appName = 'Zavora';
  static const String defaultLocale = 'en';
  static const int pageSize = 20;
  static const Duration requestTimeout = Duration(seconds: 30);
}
